/*
*
*    @ h.rasouli
*
*/
import {ModuleWithProviders} from '@angular/core';
import {RouterModule, Routes} from '@angular/router';

export const passwordHistoryRoutesArr: Routes = [];

export const PasswordHistoryRoute: ModuleWithProviders = RouterModule.forChild(passwordHistoryRoutesArr);
