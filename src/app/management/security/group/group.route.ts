/**
 *
 *    @ AH.GHORAB
 *
 */
import {ModuleWithProviders} from '@angular/core';
import {RouterModule, Routes} from '@angular/router';

export const groupRoutesArr: Routes = [];

export const GroupRoute: ModuleWithProviders = RouterModule.forChild(groupRoutesArr);
