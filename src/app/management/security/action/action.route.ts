/**
 * @ M.AMERY
 */
import {RouterModule, Routes} from '@angular/router';
import {ModuleWithProviders} from '@angular/core';

const routes: Routes = [];
export const ActionRoute: ModuleWithProviders = RouterModule.forChild(routes);
